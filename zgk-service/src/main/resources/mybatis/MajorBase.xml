<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.gk.dao.IForecastDAO">

    <!--查询专业大类-->
    <select id="getMajoredCategoryByMajorType" resultType="java.util.Map">
        SELECT
        discipline_code,discipline_name
        from zgk_major_base
        <where>
            <if test="majorType!=null">
                major_type = #{majorType}
            </if>
        </where>
        GROUP BY discipline_code
    </select>

    <!--查询专业大类-->
    <select id="getCategoryMajoredByCategoryId" resultType="java.util.Map">
        SELECT
        discipline_sub_code,
        discipline_sub_name
        from zgk_major_base
        WHERE discipline_code=#{categoryId}
        GROUP BY discipline_sub_code
    </select>

    <!--查询专业大类-->
    <select id="getMajoredByName" resultType="java.util.Map">
        SELECT
        discipline_sub_code,
        discipline_sub_name
        from zgk_major_base
        WHERE discipline_code=#{categoryId}
        GROUP BY discipline_sub_code
    </select>
</mapper>

