<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.gk.dao.IInformationDAO">
     <select id="getAllInformation" resultType="Information">
         SELECT id,hotInformation,informationContent FROM gk_gkinformation_gkhot
         WHERE  status<![CDATA[<>]]>  -1 ORDER BY createDate ASC
         limit ${offset}, ${rows}
     </select>
    <select id="getInformationByKey" parameterType="java.lang.String" resultType="Information">
        select id,hotInformation,informationContent FROM gk_gkinformation_gkhot
        where hotInformation like CONCAT('%', #{key} , '%') and status <![CDATA[<>]]> -1
    </select>

</mapper>

